# services/ — wrappers for external APIs
# email_resend.py  — Resend API (transactional + nurture emails)
# telegram.py      — Telegram Bot API (one-time invite links)
# stripe_client.py — Stripe API (payment links, webhook verification)
